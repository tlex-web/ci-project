// Invoke utility functions
setDate();

document.addEventListener('DOMContentLoaded', start);

document.querySelector('#contact').addEventListener('submit', processForm);

document.addEventListener('DOMContentLoaded', getArticles);
